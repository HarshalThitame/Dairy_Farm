"use client";

import MonthSelector from "@/components/MonthSelector";

export default function AccountingMonthSelector({ value, onChange }) {
  return <MonthSelector value={value} onChange={onChange} />;
}
